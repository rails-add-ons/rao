en:
  rao:
    service_controller:
      base:
        form_actions:
          submit: "Execute %{service_name}"
        form_errors:
          heading:
            one: "An error occurred while executing %{service_name}"
            other: "%{count} errors occurred while executing %{service_name}"
        new:
          title: "Execute %{service_name}"
        new_actions:
          back: "Back"
        result:
          title: "%{service_name} executed"
        result_actions:
          back: "Back"
